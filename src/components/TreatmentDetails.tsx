import { View, Text } from "react-native";
import React from "react";

const TreatmentDetails = ({ item }: any) => {
  console.log(item);

  return (
    <View>
      <Text>TreatmentDetails</Text>
    </View>
  );
};

export default TreatmentDetails;
